@model IEnumerable<_04_Data.Datos.frase>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.frase1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.actor.actor_principal)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.pelicula.titulo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.frase1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.actor.actor_principal)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.pelicula.titulo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
